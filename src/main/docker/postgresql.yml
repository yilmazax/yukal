version: '2'
services:
  yukal-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/yukal/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=yukal
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
